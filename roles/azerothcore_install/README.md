# azerothcore_install

Prepares the base AzerothCore Docker project used by the rest of the collection.

This role sets up the local `acore-docker` project structure and generated
Compose configuration so that modules can be mounted, images can be built, and
the stack can later be deployed.

See the collection README for supported variables.

## License

GPL-3.0-only
